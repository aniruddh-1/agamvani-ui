<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- HTTPS-only configuration for production domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">av.ramsabha.in</domain>
        <domain includeSubdomains="true">ramsabha.in</domain>
        <!-- Trust system certificates -->
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>
    
    <!-- Allow localhost for development only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain> <!-- Android emulator host -->
    </domain-config>
    
    <!-- Base configuration: HTTPS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
